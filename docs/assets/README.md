# Screenshots needed for the main README

Capture from the `development` vault (light or dark theme — pick one and keep it consistent), window ~1200px wide, and save here with these names:

| File | What to capture |
|---|---|
| `dashboard.png` | Binder view: stat tiles + portfolio chart + collection/deck lists |
| `collection.png` | Collection view of a real collection: thumbnails, condition badges, playset pill, filters and set progress visible |
| `deck.png` | Deck view of the imported tournament deck: groups with thumbnails, green validation panel, missing-from-collection section |

Optional extras that sell well: `search.gif` (Add cards loop with the keep-searching toggle) and `import.gif` (pasting a TCG Live list). Obsidian's built-in window + any GIF recorder (e.g. ScreenToGif) works.
